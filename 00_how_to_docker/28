# doesn't run through daemon have to run:
#   docker-machine ssh Char
docker service create --env OC_USERNAME=elena --env OC_PASSWD=privet --network overmind --replicas 2 --name marines 42school/marine-squad
